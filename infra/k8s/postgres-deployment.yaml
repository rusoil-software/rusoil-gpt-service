apiVersion: apps/v1
kind: Deployment
metadata:
  name: petra-gpt-postgres
  labels:
    app: petra-gpt-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: petra-gpt-postgres
  template:
    metadata:
      labels:
        app: petra-gpt-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: petra-gpt-secrets
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: petra-gpt-secrets
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              value: "petra-gpt"
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: pgdata
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: pgdata
          emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: petra-gpt-postgres-svc
spec:
  selector:
    app: petra-gpt-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
