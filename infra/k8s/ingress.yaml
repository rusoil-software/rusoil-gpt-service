apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: usptu-ingress
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "20m"
spec:
  ingressClassName: nginx
  rules:
    - host: usptu.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: usptu-backend-svc
                port:
                  number: 8000
