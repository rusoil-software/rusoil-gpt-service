apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rusoilgpt-ingress
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "20m"
spec:
  ingressClassName: nginx
  rules:
    - host: rusoilgpt.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rusoilgpt-backend-svc
                port:
                  number: 8000
