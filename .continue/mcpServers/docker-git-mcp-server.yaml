name: Docker MCP Git
version: 0.0.2
schema: v1

mcpServers:
  - name: Git
    command: docker
    args:
      - run
      - --rm
      - -i
      - --mount
      - type=bind,src=${{ inputs.GIT_DIR }},dst=${{ inputs.GIT_DIR }}
      - mcp/git
