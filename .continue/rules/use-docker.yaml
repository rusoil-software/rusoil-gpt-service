name: Use Docker
version: 0.0.1
schema: v1
rules:
  - >-
    # Use Docker


    You should use Docker in this project. 


    When choosing images from the Docker Hub make sure that you are using the
    latest version unless otherwise instructed 


    Use your web search lookup tool if you're not certain of the correct image
    name or if the image name appears to be failing. 
