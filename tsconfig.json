{
  "name": "petra-gpt-service",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "jest --runInBand",
    "test:watch": "jest --watchAll"
  },
  "devDependencies": {
    "typescript": "5.2.0",
    "jest": "29.6.0",
    "ts-jest": "29.1.0",
    "@types/jest": "29.5.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.ts",
      "**/tests/**/*.spec.ts"
    ]
  }
}
